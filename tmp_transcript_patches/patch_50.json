{
    "line": 141,
    "old": "        function placeInTag(wrap, canvasEl, halfZone) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.margin = '0';\n            wrap.style.right = 'auto';\n            var w = Math.max(40, wrap.offsetWidth || 80);\n            wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n            var mid = Math.floor(canvasEl.clientHeight * 0.5);\n            var h = Math.max(20, wrap.offsetHeight || 24);\n            if (isDual120 && halfZone === 'bottom') {\n                wrap.style.top = Math.min(canvasEl.clientHeight - h - pad, mid + 2) + 'px';\n            } else if (isDual120 && halfZone === 'top') {\n                wrap.style.top = Math.min(pad, Math.max(pad, mid - h - 2)) + 'px';\n            } else {\n                wrap.style.top = pad + 'px';\n            }\n        }\n        placeInTag(bc1, c1, isDual120 ? 'top' : 'none');\n        placeInTag(bc2, c2, isDual120 ? 'bottom' : 'none');",
    "new": "        function placeInTag(wrap, canvasEl) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.margin = '0';\n            wrap.style.right = 'auto';\n            var w = Math.max(40, wrap.offsetWidth || 80);\n            wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n            var h = Math.max(20, wrap.offsetHeight || 24);\n            if (isDual120) {\n                wrap.style.top = Math.max(pad, Math.round((canvasEl.clientHeight - h) \/ 2)) + 'px';\n            } else {\n                wrap.style.top = pad + 'px';\n            }\n        }\n        placeInTag(bc1, c1);\n        placeInTag(bc2, c2);"
}