{
    "line": 129,
    "old": "    function positionDefaultBarcodesInEachLabel() {\n        var bc1 = document.getElementById('barcode1');\n        var bc2 = document.getElementById('barcode2');\n        var c1 = labelCanvas1 || document.getElementById('labelCanvas1');\n        var c2 = labelCanvas2 || document.getElementById('labelCanvas2');\n        var isDual120 = isDualLabelLayoutPreset();\n        var pad = isDual120 ? 4 : 6;\n        function placeInTag(wrap, canvasEl, alignRight) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.top = pad + 'px';\n            wrap.style.margin = '0';\n            if (alignRight) {\n                var w = Math.max(40, wrap.offsetWidth || 80);\n                wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n                wrap.style.right = 'auto';\n            } else {\n                wrap.style.left = pad + 'px';\n                wrap.style.right = 'auto';\n            }\n        }\n        placeInTag(bc1, c1, isDual120);\n        placeInTag(bc2, c2, isDual120);\n        if (typeof renderCanvasBarcode === 'function') renderCanvasBarcode();\n        if (typeof clampBarcodeBlockIntoCanvas === 'function') {\n            if (isDual120) {\n                [bc1, bc2].forEach(function(wrap, idx) {\n                    var canvasEl = idx === 0 ? c1 : c2;\n                    if (!wrap || !canvasEl) return;\n                    var maxTop = Math.max(pad, Math.floor(canvasEl.clientHeight * 0.5) - (wrap.offsetHeight || 24) - 2);\n                    var topVal = parseFloat(wrap.style.top) || pad;\n                    wrap.style.top = Math.min(topVal, maxTop) + 'px';\n                });\n            } else {\n                clampBarcodeBlockIntoCanvas(c1, bc1);\n                clampBarcodeBlockIntoCanvas(c2, bc2);\n            }\n        }\n        syncDualCanvasHeight();\n    }",
    "new": "    function positionDefaultBarcodesInEachLabel() {\n        var bc1 = document.getElementById('barcode1');\n        var bc2 = document.getElementById('barcode2');\n        var c1 = labelCanvas1 || document.getElementById('labelCanvas1');\n        var c2 = labelCanvas2 || document.getElementById('labelCanvas2');\n        var isDual120 = isDualLabelLayoutPreset();\n        var pad = isDual120 ? 4 : 6;\n        function placeInTag(wrap, canvasEl, halfZone) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.margin = '0';\n            wrap.style.right = 'auto';\n            var w = Math.max(40, wrap.offsetWidth || 80);\n            wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n            var mid = Math.floor(canvasEl.clientHeight * 0.5);\n            var h = Math.max(20, wrap.offsetHeight || 24);\n            if (isDual120 && halfZone === 'bottom') {\n                wrap.style.top = Math.min(canvasEl.clientHeight - h - pad, mid + 2) + 'px';\n            } else if (isDual120 && halfZone === 'top') {\n                wrap.style.top = Math.min(pad, Math.max(pad, mid - h - 2)) + 'px';\n            } else {\n                wrap.style.top = pad + 'px';\n            }\n        }\n        placeInTag(bc1, c1, isDual120 ? 'top' : 'none');\n        placeInTag(bc2, c2, isDual120 ? 'bottom' : 'none');\n        if (typeof renderCanvasBarcode === 'function') renderCanvasBarcode();\n        if (!isDual120 && typeof clampBarcodeBlockIntoCanvas === 'function') {\n            clampBarcodeBlockIntoCanvas(c1, bc1);\n            clampBarcodeBlockIntoCanvas(c2, bc2);\n        }\n        syncDualCanvasHeight();\n    }"
}