{
    "line": 159,
    "old": "        function placeInTag(wrap, canvasEl, hAlign) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.margin = '0';\n            var w = Math.max(40, wrap.offsetWidth || 80);\n            var h = Math.max(20, wrap.offsetHeight || 24);\n            if (hAlign === 'left') {\n                wrap.style.left = pad + 'px';\n                wrap.style.right = 'auto';\n            } else {\n                wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n                wrap.style.right = 'auto';\n            }\n            if (isDual120) {\n                wrap.style.top = pad + 'px';\n            } else {\n                wrap.style.top = pad + 'px';\n            }\n        }\n        placeInTag(bc1, c1, isDual120 ? 'left' : 'right');\n        placeInTag(bc2, c2, isDual120 ? 'right' : 'right');",
    "new": "        function placeInTag(wrap, canvasEl, hAlign, vZone) {\n            if (!wrap || !canvasEl) return;\n            resetBarcodeWrapInlineSize(wrap);\n            wrap.style.position = 'absolute';\n            wrap.style.margin = '0';\n            var w = Math.max(40, wrap.offsetWidth || 80);\n            var h = Math.max(20, wrap.offsetHeight || 24);\n            var mid = Math.floor(canvasEl.clientHeight * 0.5);\n            if (hAlign === 'left') {\n                wrap.style.left = pad + 'px';\n                wrap.style.right = 'auto';\n            } else {\n                wrap.style.left = Math.max(pad, canvasEl.clientWidth - w - pad) + 'px';\n                wrap.style.right = 'auto';\n            }\n            if (vZone === 'bottom') {\n                wrap.style.top = Math.max(mid + 2, canvasEl.clientHeight - h - pad) + 'px';\n            } else if (vZone === 'top') {\n                wrap.style.top = pad + 'px';\n            } else {\n                wrap.style.top = pad + 'px';\n            }\n        }\n        \/\/ Left preview tag = bottom-left on sticker; right preview tag = top-right on sticker\n        placeInTag(bc1, c1, 'left', isDual120 ? 'bottom' : null);\n        placeInTag(bc2, c2, 'right', isDual120 ? 'top' : null);"
}