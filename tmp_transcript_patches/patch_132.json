{
    "line": 441,
    "old": "    var DUAL_LABEL_SIZES = ['120x50', '250x120'];\n    var DUAL_TAG_GAP_MM = 2;\n\n    function getDualPresetConfig(val) {",
    "new": "    var DUAL_LABEL_SIZES = ['120x50', '250x120'];\n    var DUAL_TAG_GAP_MM = 2;\n    var DUAL_120x50_SCREEN_CM = { w: 8.2, h: 3.8 };\n\n    function isDualUnified120x50(val) {\n        return (val || barcodeLabelPreset()) === '120x50';\n    }\n    function measureDesignerPxPerMm() {\n        var probe = document.createElement('div');\n        probe.style.cssText = 'position:fixed;left:-9999px;top:0;width:100mm;height:1px;visibility:hidden;';\n        document.documentElement.appendChild(probe);\n        var w = probe.getBoundingClientRect().width;\n        probe.remove();\n        return (w > 0) ? (w \/ 100) : MM_TO_PX;\n    }\n    function designerPhysicalScreenScale(axis) {\n        var dpr = window.devicePixelRatio || 1;\n        if (dpr >= 1.05) return dpr;\n        return axis === 'h' ? (DUAL_120x50_SCREEN_CM.h \/ 3.1) : (DUAL_120x50_SCREEN_CM.w \/ 6.8);\n    }\n    \/** Match auragold_120x50_quadrant_origin_mm() in config.php *\/\n    function getDual120x50PocketsMm() {\n        var stickerW = 120, stickerH = 50, gap = DUAL_TAG_GAP_MM;\n        var half = (stickerW - gap) \/ 2;\n        var strip = 6, handle = 10;\n        var whiteW = Math.max(8, half - strip - handle);\n        var boxW = 20, boxH = 25;\n        var foldY = stickerH \/ 2;\n        var top = Math.max(foldY, stickerH - boxH);\n        var leftWhiteX = strip;\n        var rightWhiteX = half + gap + strip;\n        return {\n            left: { left: leftWhiteX + (whiteW - boxW) \/ 2, top: top, w: boxW, h: boxH },\n            right: { left: rightWhiteX + (whiteW - boxW) \/ 2, top: top, w: boxW, h: boxH }\n        };\n    }\n    function layoutDualUnified120x50Preview() {\n        var frame = document.getElementById('barcodeDualOuterFrame');\n        var sheet = document.getElementById('barcodeDualSheetInner');\n        var block1 = document.getElementById('barcodeLabel1');\n        var block2 = document.getElementById('barcodeLabel2');\n        if (!frame || !sheet) return;\n        var pxPerMm = measureDesignerPxPerMm();\n        var scaleW = designerPhysicalScreenScale('w');\n        var scaleH = designerPhysicalScreenScale('h');\n        var outerWPx = Math.round(DUAL_120x50_SCREEN_CM.w * 10 * pxPerMm * scaleW);\n        var outerHPx = Math.round(DUAL_120x50_SCREEN_CM.h * 10 * pxPerMm * scaleH);\n        var sheetWPx = Math.round(120 * pxPerMm);\n        var sheetHPx = Math.round(50 * pxPerMm);\n        var scaleX = sheetWPx > 0 ? (outerWPx \/ sheetWPx) : 1;\n        var scaleY = sheetHPx > 0 ? (outerHPx \/ sheetHPx) : 1;\n        frame.style.width = outerWPx + 'px';\n        frame.style.height = outerHPx + 'px';\n        frame.style.minWidth = outerWPx + 'px';\n        frame.style.minHeight = outerHPx + 'px';\n        sheet.style.width = sheetWPx + 'px';\n        sheet.style.height = sheetHPx + 'px';\n        sheet.style.transform = 'scale(' + scaleX + ',' + scaleY + ')';\n        sheet.style.transformOrigin = 'top left';\n        var pockets = getDual120x50PocketsMm();\n        function placeBlock(block, pocket) {\n            if (!block) return;\n            block.style.left = Math.round(pocket.left * pxPerMm) + 'px';\n            block.style.top = Math.round(pocket.top * pxPerMm) + 'px';\n            block.style.width = Math.round(pocket.w * pxPerMm) + 'px';\n            block.style.height = Math.round(pocket.h * pxPerMm) + 'px';\n        }\n        placeBlock(block1, pockets.left);\n        placeBlock(block2, pockets.right);\n        var rw = document.getElementById('barcodeDualOuterRulerW');\n        var rh = document.getElementById('barcodeDualOuterRulerH');\n        if (rw) rw.textContent = 'W = ' + DUAL_120x50_SCREEN_CM.w.toFixed(1) + ' cm';\n        if (rh) rh.textContent = 'H = ' + DUAL_120x50_SCREEN_CM.h.toFixed(1) + ' cm';\n    }\n\n    function getDualPresetConfig(val) {"
}